<UserControl x:Class="Alfred.App.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             x:Name="Root">
  <StackPanel>
    <Grid Height="56" Margin="28,0,22,0" shell:WindowChrome.IsHitTestVisibleInChrome="True">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Image Source="{Binding Icon, ElementName=Root}" Width="20" Height="20" Margin="0,1,10,0"
               VerticalAlignment="Center" />
        <TextBlock Text="{Binding Title, ElementName=Root}"
                   FontFamily="{StaticResource AppFontDisplay}"
                   FontSize="19"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimary}"
                   VerticalAlignment="Center" />
        <Border CornerRadius="8" Background="{DynamicResource SheetHover}" Padding="8,1,8,2"
                Margin="10,2,0,0" VerticalAlignment="Center">
          <Border.Style>
            <Style TargetType="Border">
              <Style.Triggers>
                <DataTrigger Binding="{Binding Count, ElementName=Root}" Value="{x:Null}">
                  <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Count, ElementName=Root}" Value="">
                  <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <TextBlock Text="{Binding Count, ElementName=Root}"
                     FontFamily="{StaticResource AppFontSmall}" FontSize="11.5" FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondary}" />
        </Border>
      </StackPanel>

      <ContentPresenter Content="{Binding Trailing, ElementName=Root}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center" />
    </Grid>
    <Border Height="1" Background="{DynamicResource Hairline}" />
  </StackPanel>
</UserControl>
