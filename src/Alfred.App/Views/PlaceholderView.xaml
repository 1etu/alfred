<UserControl x:Class="Alfred.App.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

    <Image Source="{StaticResource AlfredCryingIcon}"
           Width="76"
           Height="76"
           HorizontalAlignment="Center"
           Opacity="0.85" />

    <TextBlock Text="Erm, we don't have it yet :("
               Margin="0,18,0,0"
               HorizontalAlignment="Center"
               FontFamily="{StaticResource AppFontDisplay}"
               FontSize="15"
               FontWeight="SemiBold"
               Foreground="{DynamicResource TextSecondary}" />

    <TextBlock Text="{Binding Title}"
               Margin="0,6,0,0"
               HorizontalAlignment="Center"
               FontFamily="{StaticResource AppFontSmall}"
               FontSize="12"
               FontWeight="Medium"
               Foreground="{DynamicResource TextSecondary}"
               Opacity="0.6" />

  </StackPanel>
</UserControl>
