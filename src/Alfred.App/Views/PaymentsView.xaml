<UserControl x:Class="Alfred.App.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Alfred.App.Controls">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
  </UserControl.Resources>

  <ScrollViewer Style="{StaticResource PageRoot}">
    <StackPanel Style="{StaticResource PageBody}">
      <TextBlock Text="Payments" Style="{StaticResource PageTitle}" />
      <TextBlock Text="{Binding Summary}" Style="{StaticResource PageSubtitle}" Margin="0,10,0,28" />

      <Grid Margin="0,0,0,20" MaxWidth="760" HorizontalAlignment="Left">
        <Border Style="{StaticResource SegmentGroup}" HorizontalAlignment="Left">
          <StackPanel Orientation="Horizontal">
            <RadioButton Style="{StaticResource Segment}" Content="All" GroupName="PayFilter" IsChecked="{Binding IsAll, Mode=TwoWay}" />
            <RadioButton Style="{StaticResource Segment}" Content="Out" GroupName="PayFilter" IsChecked="{Binding IsOut, Mode=TwoWay}" />
            <RadioButton Style="{StaticResource Segment}" Content="Recurring" GroupName="PayFilter" IsChecked="{Binding IsRecurring, Mode=TwoWay}" />
            <RadioButton Style="{StaticResource Segment}" Content="In" GroupName="PayFilter" IsChecked="{Binding IsIn, Mode=TwoWay}" />
          </StackPanel>
        </Border>
      </Grid>

      <Border x:Name="Composer"
              CornerRadius="10"
              Background="{DynamicResource CardBackground}"
              Padding="14"
              Margin="0,0,0,16"
              Visibility="Collapsed">
        <StackPanel>
          <controls:SmartInput x:Name="ComposeTitle" Placeholder="What is it? Netflix, rent, freelance…" />
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="120" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="130" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" CornerRadius="8" Background="{DynamicResource SegmentTrack}">
              <TextBox x:Name="ComposeAmount"
                       Margin="8,0,8,0" Padding="3,8,3,8"
                       Background="Transparent" BorderThickness="0"
                       FontFamily="{StaticResource AppFont}" FontSize="{StaticResource BodyFontSize}"
                       Foreground="{DynamicResource TextPrimary}" CaretBrush="{DynamicResource TextPrimary}" />
            </Border>
            <controls:SmartInput x:Name="ComposeCategory" Grid.Column="1" Margin="8,0,8,0" Placeholder="Category" VerticalAlignment="Top" />
            <controls:SmartInput x:Name="ComposeDate" Grid.Column="2" Placeholder="When" VerticalAlignment="Top" />
          </Grid>
          <Grid Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
              <Border Style="{StaticResource SegmentGroup}">
                <StackPanel Orientation="Horizontal">
                  <RadioButton x:Name="KindExpense" Style="{StaticResource Segment}" Content="Expense" GroupName="Kind" IsChecked="True" />
                  <RadioButton x:Name="KindPayment" Style="{StaticResource Segment}" Content="Payment" GroupName="Kind" />
                  <RadioButton x:Name="KindIncome" Style="{StaticResource Segment}" Content="Income" GroupName="Kind" />
                </StackPanel>
              </Border>
              <Border Style="{StaticResource SegmentGroup}" Margin="8,0,0,0">
                <StackPanel Orientation="Horizontal">
                  <RadioButton x:Name="CadenceOnce" Style="{StaticResource Segment}" Content="Once" GroupName="Cadence" IsChecked="True" />
                  <RadioButton x:Name="CadenceWeekly" Style="{StaticResource Segment}" Content="Weekly" GroupName="Cadence" />
                  <RadioButton x:Name="CadenceMonthly" Style="{StaticResource Segment}" Content="Monthly" GroupName="Cadence" />
                  <RadioButton x:Name="CadenceYearly" Style="{StaticResource Segment}" Content="Yearly" GroupName="Cadence" />
                </StackPanel>
              </Border>
            </StackPanel>
            <Button Style="{StaticResource GhostButton}" HorizontalAlignment="Right" Click="OnCommit">
              <TextBlock Text="Add it" FontFamily="{StaticResource AppFont}" FontSize="12" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextPrimary}" />
            </Button>
          </Grid>
        </StackPanel>
      </Border>

      <ItemsControl ItemsSource="{Binding Rows}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border x:Name="Row" Style="{StaticResource ItemRow}">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="24" Height="24" CornerRadius="6"
                        Margin="0,0,10,0" VerticalAlignment="Center"
                        Background="{DynamicResource CardBackground}">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding BrandSlug}" Value="{x:Null}">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <Image Source="{Binding BrandSlug, Converter={StaticResource BrandTileImage}}" Width="14" Height="14" />
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                  <TextBlock Text="{Binding Title}" Style="{StaticResource RowTitle}" />
                  <TextBlock Text="{Binding Meta}" Style="{StaticResource RowMeta}" Margin="0,1,0,0" />
                </StackPanel>

                <TextBlock Grid.Column="2" Margin="14,0,0,0" Text="{Binding Amount}">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource RowAmount}">
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsIncome}" Value="True">
                          <Setter Property="Foreground" Value="{DynamicResource MoneyIn}" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>

                <Button Grid.Column="4" Margin="8,0,0,0" Click="OnRemoveRow">
                  <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                      <Setter Property="Opacity" Value="0" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMouseOver, ElementName=Row}" Value="True">
                          <Setter Property="Opacity" Value="1" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Button.Style>
                  <Viewbox Width="13" Height="13">
                    <Canvas Width="24" Height="24">
                      <Path Data="{StaticResource TrashGlyph}" Fill="{DynamicResource TextSecondary}" />
                    </Canvas>
                  </Viewbox>
                </Button>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <StackPanel HorizontalAlignment="Center" Margin="0,120,0,0" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
        <Image Source="{StaticResource AlfredCryingIcon}" Width="80" Height="80" Opacity="0.4" />
        <TextBlock Text="No money moving yet. Add the first one."
                   Margin="0,14,0,0" HorizontalAlignment="Center"
                   FontFamily="{StaticResource AppFont}" FontSize="14"
                   Foreground="{DynamicResource TextSecondary}" />
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
