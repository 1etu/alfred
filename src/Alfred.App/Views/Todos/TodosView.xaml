<UserControl x:Class="Alfred.App.Views.TodosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Alfred.UIKit.Controls;assembly=Alfred.UIKit">
  <UserControl.Resources>
    <DataTemplate x:Key="TodoRowTemplate">
      <Border x:Name="Row" Style="{StaticResource ItemRow}" MinHeight="40">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <ToggleButton Style="{StaticResource CheckCircle}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        IsChecked="{Binding IsDone, Mode=TwoWay}" />

          <Border Grid.Column="1"
                  CornerRadius="5"
                  Background="{DynamicResource SegmentTrack}"
                  Padding="7,1,7,2"
                  Margin="0,0,9,0"
                  VerticalAlignment="Center"
                  Visibility="{Binding HasDue, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{Binding Due}"
                       FontFamily="{StaticResource AppFontSmall}"
                       FontSize="11"
                       FontWeight="SemiBold">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                      <Setter Property="Foreground" Value="{DynamicResource Overdue}" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
          </Border>

          <TextBlock Grid.Column="2" Text="{Binding Title}" VerticalAlignment="Center">
            <TextBlock.Style>
              <Style TargetType="TextBlock" BasedOn="{StaticResource RowTitle}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsDone}" Value="True">
                    <Setter Property="Opacity" Value="0.45" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>

          <Button Grid.Column="3" Click="OnRemoveRow" AutomationProperties.Name="{DynamicResource Loc.Trash.Forget}">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource RowActionButton}">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsMouseOver, ElementName=Row}" Value="True">
                    <Setter Property="Opacity" Value="1" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
            <Viewbox Width="12" Height="12">
              <Canvas Width="24" Height="24">
                <Path Data="{StaticResource TrashGlyph}" Fill="{DynamicResource TextSecondary}" />
              </Canvas>
            </Viewbox>
          </Button>
        </Grid>
      </Border>
    </DataTemplate>
  </UserControl.Resources>

  <ScrollViewer Style="{StaticResource PageRoot}">
    <StackPanel Style="{StaticResource PageBody}">

      <StackPanel Orientation="Horizontal" Margin="0,44,0,16">
        <Image Source="{Binding Icon}" Width="24" Height="24" Margin="0,3,10,0" VerticalAlignment="Center" />
        <TextBlock Text="{Binding Title}" Style="{StaticResource BigTitle}" />
      </StackPanel>

      <controls:QuickBar x:Name="Bar"
                         TitlePlaceholder="{DynamicResource Loc.Todos.Add}"
                         Submitted="OnQuickAdd"
                         Margin="0,0,0,18" />

      <ItemsControl ItemsSource="{Binding Open}" ItemTemplate="{StaticResource TodoRowTemplate}" />

      <TextBlock Text="{DynamicResource Loc.Agenda.Empty}"
                 Style="{StaticResource RowMeta}"
                 Margin="12,10,0,0"
                 Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}" />

      <StackPanel Margin="0,22,0,0"
                  Visibility="{Binding HasLogbook, Converter={StaticResource BoolToVisibility}}">
        <controls:SectionHeader Title="{Binding LogbookHeader}" />
        <ItemsControl ItemsSource="{Binding Logbook}" ItemTemplate="{StaticResource TodoRowTemplate}" Opacity="0.6" />
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
