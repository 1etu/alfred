<UserControl x:Class="Alfred.UIKit.Controls.WindowButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
  <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

    <Button Style="{StaticResource WindowButton}"
            Background="{DynamicResource WindowButtonClose}"
            Content="{StaticResource CloseGlyph}"
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="OnCloseClick" />

    <Button Style="{StaticResource WindowButton}"
            Background="{DynamicResource WindowButtonMinimize}"
            Content="{StaticResource MinimizeGlyph}"
            Margin="8,0,0,0"
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="OnMinimizeClick" />

    <Button Style="{StaticResource WindowButton}"
            Background="{DynamicResource WindowButtonZoom}"
            Content="{StaticResource ZoomGlyph}"
            Margin="8,0,0,0"
            shell:WindowChrome.IsHitTestVisibleInChrome="True"
            Click="OnZoomClick" />

  </StackPanel>
</UserControl>
