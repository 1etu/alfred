<UserControl x:Class="Alfred.UIKit.Controls.KeycapHintBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Alfred.UIKit.Controls"
             x:Name="Root"
             Focusable="False"
             IsTabStop="False">
  <ItemsControl ItemsSource="{Binding Hints, ElementName=Root}">
    <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
        <StackPanel Orientation="Horizontal" />
      </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate>
      <DataTemplate DataType="{x:Type controls:KeyHint}">
        <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
          <Border Style="{StaticResource KeyCap}" Padding="6,2,6,3">
            <TextBlock Text="{Binding Key}" Style="{StaticResource KeyCapText}" />
          </Border>
          <TextBlock Text="{Binding Action}"
                     Margin="5,0,0,0"
                     VerticalAlignment="Center"
                     FontFamily="{StaticResource AppFontSmall}"
                     FontSize="11"
                     Foreground="{DynamicResource TextSecondary}" />
        </StackPanel>
      </DataTemplate>
    </ItemsControl.ItemTemplate>
  </ItemsControl>
</UserControl>
